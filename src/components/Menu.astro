---
interface Props {
    useTitleAnim?: boolean;
}

const { useTitleAnim } = Astro.props;
---

<nav
    class="navbar navbar-dark bg-dark fixed-top navbar-expand-lg bg-body-tertiary block-drag"
>
    <div class="container-fluid">
        <a class="navbar-brand" href="/2024/">
            <img
                src="/2024/image/logo.png"
                alt="튜토리얼"
                width="35"
                height="35"
            />

            <div class={useTitleAnim ? "transparent" : ""}>
                국립공주대학교 게임디자인학과<br />
                <strong>제20회 졸업전시회</strong>
            </div>
        </a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/2024/about">소개</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/2024/project">프로젝트</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/2024/guestbook">방명록</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<script is:inline>
    if (useTitleAnim == false) return;

    let title = document.querySelector(".navbar-brand div");
    document.body.addEventListener("scroll", (e) => {
        if (document.body.scrollTop >= 150)
            title.classList.remove("transparent");
        else title.classList.add("transparent");
    });
</script>

<style>
    .bg-body-tertiary {
        background-color: #ffffff75 !important;
        backdrop-filter: blur(10px);
        /* box-shadow: 2px 2px 10px rgba(32, 32, 32, 0.242); */
    }

    .container-fluid {
        max-width: 1000px;
        padding: 0;
    }

    .container-fluid {
        max-width: 1000px;
        margin: 0 auto;
    }

    @media (width <= 1000px) {
        .container-fluid {
            padding: 0 40px;
        }
    }

    @media (width <= 600px) {
        .container-fluid {
            padding: 0 20px;
        }
    }

    .navbar-brand {
        display: flex;
        flex-direction: row;
        column-gap: 1rem;
        align-items: center;
    }

    .navbar-brand div {
        color: var(--black);
        font-size: 0.9rem;
        line-height: 1.2rem;
        transition: color 0.1s ease-in;
    }
    .navbar-brand .transparent {
        color: #ffffff00;
        transition: color 0.1s ease-out;
    }

    .navbar-collapse {
        flex-grow: 0;
    }

    .navbar-toggler {
        filter: invert(1) brightness(0);
    }

    .nav-link,
    .nav-link:hover {
        font-weight: 500;
        font-size: 18px;
        padding-left: 1.2rem !important;
        padding-right: 1.2rem !important;
        color: var(--black);
    }

    @media (width <= 350px) {
        .navbar-brand div {
            display: none;
        }
    }
</style>
